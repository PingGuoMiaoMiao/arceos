# LicheeRV Nano WE / SG2002 项目 Session 记录

## 1. Session 元数据

| 字段 | 值 |
| --- | --- |
| 日期 | 2026-09-25（Asia/Shanghai） |
| 项目 | ArceOS 移植到 LicheeRV Nano WE（SG2002） |
| 权威工作树 | `/home/chen/arceos-worktrees/sg2002-phone-tpu` |
| Git 分支 | `codex/sg2002-phone-tpu` |
| Git 远端 | `personal` → `https://github.com/PingGuoMiaoMiao/arceos.git` |
| 本次记录前的 HEAD | `627cdc1a9f38edfa70802eaba14361b4a6e0225e` |
| 参考 Session 包 | `C:\Users\chen\Downloads\dsh-session-session-2aadc18f-8888-44df-a803-a12c9bcce2b4.zip` |
| 参考包 SHA-256 | `C33BD9289C5FEB361468C3FA35AF99276F2A717DA2AAEAE1C4ED6A5352821320` |
| 项目复诊 Session 包 | `C:\Users\chen\Downloads\dsh-session-session-c7598b32-8049-4c8f-8c03-656055a82b4f.zip` |
| 项目复诊包 SHA-256 | `D30539F5391D26BE2393E23D2EB1E7E8FD309423D415D1B0B0E55BE76D19049A` |

第一个参考包内含主 `session.jsonl` 与一个 `subagents/.../session.jsonl`。第二个项目复诊包只含 `session.jsonl`，Session ID 为 `session-c7598b32-8049-4c8f-8c03-656055a82b4f`，工作目录正是 `C:\Users\chen\Documents\ArcOS移植sg2002`，共 7,388 条事件。它们保存机器可追踪的会话元数据、消息、工具调用、工具结果、错误、重试和任务状态。

本项目采用四层记录：原始 Session ZIP、原始 UART/HTTP 证据、人可读交接与操作日志、Git 提交。文档记录“观察、依据、动作、结果和下一步”，不保存隐藏的逐字思维过程，也不复制与项目无关的原始消息。

## 2. 本次 Session 目标

1. 确认 WSL、ArceOS 权威工作树和 Git 远端是否仍可用。
2. 检查 CH340 上电后的 UART 采集结果。
3. 固化可复用的开发板 UART 识别与采集流程。
4. 形成当前项目进度、证据边界和下一验收门禁。

## 3. 本次检查结果

### 3.1 WSL 与文件位置

- `Ubuntu` 发行版可以启动，运行于 WSL2，用户为 `chen`。
- ArceOS 权威工作树存在：`/home/chen/arceos-worktrees/sg2002-phone-tpu`。
- 工作树已快进同步到远端 `personal/codex/sg2002-phone-tpu`，同步后状态干净。
- 当前 Ubuntu 的注册位置是：`C:\Users\chen\AppData\Local\Packages\CanonicalGroupLimited.Ubuntu_79rhkp1fndgsc\LocalState`。
- `D:\Recovered_WSL\f831978898.vhdx` 仍存在，文件大小为 `5,955,824,640` 字节，不能在内容核验和备份完成前删除。
- 本次检查时 `E:\WSL\Ubuntu-22.04` 不存在，`E:\WSL` 中未列出发行版文件。
- 本次检查时 `C:\Users\chen\Desktop\备份` 不存在；这只表示该路径当前未找到，不表示其中内容已被证明删除。

### 3.2 工具链

在权威工作树内读取 `rust-toolchain` 后得到：

- `rustc 1.89.0-nightly (60dabef95 2025-05-19)`
- `cargo 1.89.0-nightly (47c911e9e 2025-05-14)`
- `cargo-axplat 0.2.2`
- `QEMU emulator version 7.0.0`

在仓库外执行 Rust 命令时，`rustup` 当前没有全局默认工具链；仓库内由固定工具链文件正常工作。

### 3.3 UART 与上电证据

- Windows 已识别 CH340 串口为 `COM3`。
- 本次完整采集文件：`C:\Users\chen\Documents\ArcOS移植sg2002\logs\licheerv-nano-full-boot-20260925-021046.raw.log`
- 文件大小：`23,326` 字节。
- SHA-256：`4A97C0438A8965A0C0265D8B6EC323E5A303D6433E646DD0B1547AFD9F49AF7C`
- 最初分类结果：`UNREADABLE`；升级诊断器后精确分类为 `NON_TEXT_SIGNAL`。

后续复诊得到以下证据链：

1. 09-14 23:48 的 `aic8800-link-up-board-20260914.log` 是最后一份可读真板日志。
2. 09-25 的多份日志具有稳定的非文本位指纹；静置与按 RESET 的日志被判为同一信号，RESET 没有带来启动文本。
3. 用户确认 CH340 的 TXD/RXD 原来接反：CH340 RXD 接到板子 RX 输入脚，接收端实际处于无驱动状态并拾取干扰。
4. 改线后连续三次采集为 0 字节；这与“板子 TX 空闲保持高电平”一致，但 0 字节本身不能证明接线正确。
5. 重新插回 CH340 后，`replugged-033115.raw.log` 和 `check-033212.raw.log` 又出现相同非文本信号。最新文件为 `check-033212.raw.log`，大小 `1,503` 字节。

因此最新物理状态仍未通过：COM3 在线，但 CH340 RXD 当前是否确实连接板子 TX，必须由新的空闲采集和按 RESET 采集共同证明。只有出现 `line_state=UART_TEXT` 才算 UART 链路恢复。

### 3.4 可复用 UART Skill

- 本机安装位置：`C:\Users\chen\.codex\skills\board-uart-capture`
- 仓库源码位置：`codex-skills/board-uart-capture`
- 自动测试：9 项全部通过（2026-09-25 重新执行）。
- 相关 Git 提交：
  - `76a3290 feat(skill): add reusable board UART capture workflow`
  - `627cdc1 fix(skill): recognize mixed UART boot logs`
  - `63358a5 feat(skill): diagnose a non-text UART line instead of guessing baud rates`
  - `738757f feat(skill): frame the regression window with a log timeline`
  - `068c789 fix(skill): clarify idle UART no-data state`

Skill 能识别 CH340、CP210x、FTDI，采集手动上电或 RESET 前后的 UART 原始日志，并将结果分类。历史 Linux 启动日志已验证混合文本识别；本次新采集仍为不可读数据。

## 4. 项目证据边界

- Git 提交、测试通过和 RISC-V 构建产物证明代码存在且能够构建，不等同于真板闭环通过。
- 真板闭环必须以 UART 中的固件启动、WPA2、DHCP、`MUSHROOM_WEB_URL`，以及 HTTP 校验器输出为证据。
- Wi-Fi 凭据只允许运行时交互输入，不能写入源代码、命令行、日志或 Git。
- Windows 外层目录 `C:\Users\chen\Documents\ArcOS移植sg2002` 当前有其他修改和日志，不作为本分支的权威源码工作树。

## 5. 本次遗留问题

1. 2026-09-25 最新线路仍产生 `NON_TEXT_SIGNAL`，必须先把 CH340 RXD 接到板子 TX，并恢复可读串口文本。
2. 尚未取得本轮真板的 `MUSHROOM_WEB_URL`。
3. 尚未取得 `/health`、有效图片推理、错误 CRC 拒绝和连续请求复用的真板证据。
4. 尚未执行真板 20 次请求稳定性验收。

## 6. 下一次 Session 的唯一入口

先解决 UART 物理接线，不继续增加 TPU、网页或网络功能。确认 `GND→GND`、`CH340 TXD→板子 RX`、`CH340 RXD→板子 TX`，VCC 不接。先做 15 秒空闲采集，再按一次 RESET 做 30 秒采集。只有得到 `line_state=UART_TEXT` 后才执行：

```powershell
& "\\wsl.localhost\Ubuntu\home\chen\arceos-worktrees\sg2002-phone-tpu\tools\sg2002\run_arceos_licheerv_nano.ps1"
```

如果从权威工作树调用，脚本精确位置是：

```text
\\wsl.localhost\Ubuntu\home\chen\arceos-worktrees\sg2002-phone-tpu\tools\sg2002\run_arceos_licheerv_nano.ps1
```

脚本准备完成并提示 `Press RESET once` 后，人工只按一次 RESET。取得 URL 后，再使用 `tools/sg2002/verify_mushroom_web.py` 执行 HTTP 与推理门禁。
